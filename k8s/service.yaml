apiVersion: v1
kind: Service
metadata:
  name: rl-recommender-service
  labels:
    app: rl-recommender
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: rl-recommender
